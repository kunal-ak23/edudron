{
  "name": "edudron-frontend",
  "version": "1.0.1",
  "private": true,
  "workspaces": [
    "apps/admin-dashboard",
    "apps/student-portal",
    "packages/ui-components",
    "packages/shared-utils"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "postinstall": "echo 'Skipping package build - apps use published packages from GitHub Packages'",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "turbo": "^1.10.12",
    "typescript": "^5.2.2"
  }
}


